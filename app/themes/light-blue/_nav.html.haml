%nav
  =link_to "Rails CMS", root_path, class: "navbar-brand"
  #main-nav
    %ul
      -@menus.find { |menu| menu.name == "Main Nav" }.menu_items.each do |item|
        %li
          =link_to item.title, item.url